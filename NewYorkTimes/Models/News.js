class News {
  constructor(id, title, imageUri, shortDescription, description) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.shortDescription = shortDescription;
    this.description = description;
  }
}
export default News;
