class News {
  constructor(id, title, imageUri, shortDescription, description,webURL) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.shortDescription = shortDescription;
		this.description = description;
		this.webURL=webURL;
  }
}
export default News;
